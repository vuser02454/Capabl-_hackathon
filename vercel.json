{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && npm ci && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm ci",
  "framework": "vite",
  "rewrites": [
    {
      "comment": "The app is a single-page hash-router build, but a visitor may still land on a deep path. Serving index.html for anything that is not a real file is what stops /worker/map returning a 404 on a cold load.",
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
